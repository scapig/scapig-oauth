application.secret=abcdefghijk

play.http.secret.key = "changeme"

play.i18n {
  langs = [ "en" ]
}

play.filters.csrf.header.bypassHeaders {
  Csrf-Token = "nocheck"
}

play.http.errorHandler = "config.ErrorHandler"

loginUrl = "http://tapi-oauth-login:7050/login"
oauthUrl = "http://tapi-oauth:7040"

services {
  application {
    host = tapi-application
    port = 7020
  }
  delegated-authority {
    host = tapi-delegated-authority
    port = 7030
  }
  requested-authority {
    host = tapi-requested-authority
    port = 7060
  }
  scope {
    host = tapi-api-scope
    port = 7010
  }
}